defmodule BinanceFuturesBot.TradeManager.TradeServerTest do
  use ExUnit.Case, async: true

  describe "&create_reversal_long/1" do
    test "calls ReversalLong to create a short"
  end

  describe "&create_reversal_short/1" do
    test "calls ReversalShort to create a short"
  end

  describe "&get_state/1" do
    test "fetches state from server"
  end

  describe "checkup_on_trade interval check" do
    test "checks up on trade in intervals"
  end
end

